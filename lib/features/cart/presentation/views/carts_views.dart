import 'package:flutter/material.dart';

class CartsViews extends StatelessWidget {
  const CartsViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Carts')));
  }
}