import 'package:flutter/material.dart';

class ForgotPassord extends StatelessWidget {
  const ForgotPassord({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Forgot Password')));
  }
}